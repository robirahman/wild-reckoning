import { createPressureTrigger } from '../factories/pressureFactory';
import { RUT_COMBAT_CONFIG } from '../data/pressureConfigs';

export const rutCombatTrigger = createPressureTrigger(RUT_COMBAT_CONFIG);
